<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:vm="clr-namespace:Fca.Mobile.ViewModels"
             x:Class="Fca.Mobile.Pages.InvoicesPage"
             x:DataType="vm:InvoicesViewModel"
             Title="Billing">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior EventName="Appearing" Command="{Binding InitializeCommand}" />
    </ContentPage.Behaviors>

    <Grid RowDefinitions="Auto,Auto,Auto,*">
        <Label Grid.Row="0"
               Padding="16,16,16,0"
               FontSize="14"
               TextColor="{StaticResource Subhead}"
               IsVisible="{Binding BillingSummaryLabel, Converter={StaticResource StringNotEmptyConverter}}"
               Text="{Binding BillingSummaryLabel}" />
        <Border Grid.Row="1" Style="{StaticResource OfflineBanner}" IsVisible="{Binding IsOffline}">
            <Label Text="No internet connection" TextColor="{StaticResource White}" HorizontalTextAlignment="Center" FontSize="13" />
        </Border>
        <Label Grid.Row="2" Style="{StaticResource ErrorLabel}" Padding="16,8,16,0" IsVisible="{Binding HasError}" Text="{Binding ErrorMessage}" />
        <RefreshView Grid.Row="3" IsRefreshing="{Binding IsRefreshing}" Command="{Binding RefreshCommand}">
            <CollectionView ItemsSource="{Binding Invoices}" SelectionMode="None">
                <CollectionView.EmptyView>
                    <VerticalStackLayout Padding="24" Spacing="8">
                        <Label Text="Billing summary" Style="{StaticResource Headline}" FontSize="22" />
                        <Label Text="Invoices and payment status from your customer portal will display here." Style="{StaticResource Subhead}" />
                    </VerticalStackLayout>
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:PortalInvoice"
                                  xmlns:models="clr-namespace:Fca.Mobile.Models;assembly=FcaMobile.Core">
                        <Border Style="{StaticResource Card}" Margin="16,8">
                            <VerticalStackLayout Spacing="4">
                                <Label Text="{Binding InvoiceName}" FontAttributes="Bold" TextColor="{StaticResource Navy}" />
                                <HorizontalStackLayout Spacing="12">
                                    <Label Text="{Binding Amount}" TextColor="{StaticResource Blue}" />
                                    <Label Text="{Binding Status}" Style="{StaticResource Subhead}" />
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
        <ActivityIndicator Grid.RowSpan="4" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="{StaticResource Blue}" VerticalOptions="Center" HorizontalOptions="Center" />
    </Grid>

</ContentPage>
